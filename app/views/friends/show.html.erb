

<div class="jumbotron">
<%= @friend.first_name %>  
<%= @friend.last_name %>
 
  <p class="lead">
  <%= @friend.email %> |
  <%= @friend.phone %> |
  <%= @friend.twitter %>
  </p>
  </br>
  </div>  

<%= link_to 'Edit', edit_friend_path(@friend), class:"btn btn-secondary" %> 
<%= link_to 'Delete', @friend, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger btn" %>
<%= link_to 'Back', friends_path, class:"btn btn-secondary" %>
